# OpenDota 高分天梯数据审计（2026-08-01）

## 结论

数据足以支持小型神经网络 MVP，并且可以训练真实的三轮选人 Policy，不必依赖随机遮挡来伪造轮次。

不建议现在训练大型 Transformer。免费接口下，严格筛选后的月度有效样本更适合低维英雄 Embedding、DeepSets/小型 MLP，以及正则化的两两统计基线。

## 查询条件

- `lobby_type = 7`：Ranked；
- `game_mode = 22`：Ranked Matchmaking / 天梯 All Pick；
- `duration >= 600`；
- `avg_rank_tier >= 70`；
- 统计窗口：截至 2026-08-01 的连续 7 个 24 小时窗口。

## 样本量

| 指标 | 结果 |
|---|---:|
| 70–75 桶日均候选局数 | 51,605 |
| 75 顶端平均桶日均候选局数 | 27,070 |
| 75 桶单日最少 / 最多 | 22,207 / 37,941 |
| 最新 100 局完整且无重复的最终 5v5 | 100 / 100 |

`avg_rank_tier=75` 不能直接解释为 Divine 5 或 Immortal。OpenDota 的 `averageMedal()` 会把 Immortal 80 转成星级后再编码，导致平均桶在 75 封顶。必须用比赛详情中的个人 `rank_tier` 二次筛选。

## 真实三轮数据

抽查最新 100 场 `avg_rank_tier=75` 的详情：

| 指标 | 结果 |
|---|---:|
| 存在 `picks_bans` | 100 / 100 |
| 可还原 2+2 / 2+2 / 1+1 | 100 / 100 |
| 无撞车尝试 | 69 |
| 1 次撞车记录 | 23 |
| 2 次撞车记录 | 8 |

还原方法：按 `order` 排序 pick 事件，删除未进入最终 10 人阵容的撞车尝试，再切成前 4、中 4、后 2。100 场均满足每阶段双方分别为 2/2、2/2、1/1。

## 真正高分局保留率

在上述 100 场顶端平均桶中：

| 严格条件 | 样本数 | 比例 |
|---|---:|---:|
| 10 人段位全部已知 | 32 | 32% |
| 10 人全部 Immortal | 17 | 17% |
| 10 人全部 Divine 或更高 | 30 | 30% |
| 至少 8 名已知 Immortal | 36 | 36% |

按 75 桶候选池粗略外推：

- 严格全员 Immortal 候选约 4,602 局/日；
- 严格全员 Divine+ 候选约 8,121 局/日。

这只是候选池估计，不等于免费接口每天能全部下载。当前 OpenDota 默认配置和响应头表明，无密钥额度为 3,000 次/日、60 次/分钟。若每个 match detail 消耗一次调用，随机抓 3,000 场 75 桶，预计每天得到约：

- 510 场严格全员 Immortal；
- 900 场严格全员 Divine+；
- 1,080 场至少 8 名 Immortal。

连续收集 30 天大约可得到 15k / 27k / 32k 三档训练集。对小型集合网络可用，对大型 Transformer 偏少。

## 数据偏差与风险

1. `public_matches` 只覆盖 OpenDota 能观察到且有足够公开段位信息的比赛，不是全部 Dota 比赛的无偏随机样本。
2. 详情中的个人段位可能有缺失，也可能是查询时的当前段位而非比赛发生时的精确快照；需要继续核对语义。
3. 最近 100 场是连续时间样本，不代表所有地区和时段。
4. 选人顺序已经可还原，但双方同轮撞英雄后的完整双边意图可能只保留一部分记录。
5. OpenDota 免费额度决定了抓取速度；正式采集器必须可续传、缓存原始响应并服从响应头限速。

## MVP 决策

先采集 10k–20k 场“至少 8 名 Immortal”数据，同时保留严格全员 Immortal 子集。之后比较：

1. 英雄基础强度基线；
2. 带收缩的两两协同/克制基线；
3. 小型 Embedding + Policy/Value 双头网络。

按时间切分训练和测试。如果神经网络没有稳定超过基线，再增加数据或模型复杂度，而不是直接上 Transformer。

